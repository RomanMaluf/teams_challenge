<% if content_for?(:page_title)%>
  <div class="content-header page-header">
    <div class="row align-items-center border-bottom mb-4">
        <h1 class="h4 pl-0 col-lg-12">
          <%= yield :page_title %>
        </h1>
    </div>
  </div>
<% end %>