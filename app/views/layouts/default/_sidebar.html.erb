<nav class="pb-3 pt-lg-3 px-1">
  <div class="text-right d-lg-none pl-3 py-2 border-bottom d-flex align-items-center">
    <div class="d-flex flex-grow-1 text-primary"></div>
  </div>
  <% if policy(:user).index? %>
    <ul class="sidebar-nav" id="sidebar-nav">
      <%= render partial: 'layouts/default/navitem', locals: {
        url: users_path,
        icon_type: 'fa-solid',
        icon: 'user',
        text: t('users.list')
      } %>
    </ul>
  <% end %>

  <% if policy(:profile).index? %>
    <ul class="sidebar-nav" id="sidebar-nav">
      <%= render partial: 'layouts/default/navitem', locals: {
        url: profiles_path,
        icon_type: 'fa-solid',
        icon: 'gears',
        text: t('profiles.list')
      } %>
    </ul>
  <% end %>

  <% if policy(:customer_account).index? %>
    <ul class="sidebar-nav" id="sidebar-nav">
      <%= render partial: 'layouts/default/navitem', locals: {
        url: customer_accounts_path,
        icon_type: 'fa-solid',
        icon: 'briefcase',
        text: t('customer_acounts.list')
      } %>
    </ul>
  <% end %>

</nav>
